# Docker compose host bind (copy to .env if you want localhost-only):
# cp .env.example .env
#
# Public EC2 / public IP (default):
DASHBOARD_HOST_BIND=0.0.0.0
#
# Local-only:
# DASHBOARD_HOST_BIND=127.0.0.1